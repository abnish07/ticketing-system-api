export async function sendVerifyEmail(email, token) {
  console.log('send verify email', email, token)
}
export async function sendPasswordResetEmail(email, token) {
  console.log('send password reset', email, token)
}
